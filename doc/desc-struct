li `Begin a deck; end with "edeck"`
li `Define a function; end with "edef"`
li `Begin loop; end with "efor"`
li `Conditional; one of: a==b,  a!=b,  a>b,  a<b,  a>=b, a<=b, a<>b c`
li `import function found in a file`
li `Include the contents of a file`
li `Begin a slide; end with "eslide"`
li `End the deck`
li `End the defintion`
li `End the for loop`
li `End the conditional`
li `Begin the else clause`
li ``
